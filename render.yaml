services:
  - type: web
    name: pos-backend
    env: docker
    plan: free
    rootDir: ./backend
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: pos-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGINS
        # Set this to your Vercel frontend URL after first deploy (e.g. https://your-app.vercel.app)
        value: "https://your-app.vercel.app"
    healthCheckPath: /actuator/health/liveness

databases:
  - name: pos-db
    plan: free
    databaseName: pos_db
    user: pos_user
